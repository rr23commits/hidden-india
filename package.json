{
  "name": "hidden-india",
  "version": "1.0.0",
  "description": "Hidden India - AI-Powered Smart Tourism Platform",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm start",
    "start:backend": "cd backend && npm start",
    "db:setup": "mysql -u root -p hidden_india < database/schema.sql"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
